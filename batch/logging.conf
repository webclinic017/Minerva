[loggers]
keys=root,batch,report

[handlers]
keys=console,stream,file,smtp,reports

[formatters]
keys=simpleFormatter,detailFomatter


[logger_root]
level=DEBUG
handlers=console

[logger_batch]
level=DEBUG
handlers=console,file
qualname=batch
propagate=0

[logger_report]
level=DEBUG
handlers=console,reports
qualname=report
propagate=0


[handler_console]
class=StreamHandler
level=INFO
formatter=simpleFormatter
args=(sys.stdout,)

[handler_stream]
class=StreamHandler
level=WARN
formatter=simpleFormatter
args=(sys.stdout,)

[handler_file]
class=FileHandler
level=WARN
formatter=detailFomatter
args=('./batch/batch.log', 'a')  # mode = 'w' 는 업데이트 모드

[handler_smtp]
class=handlers.SMTPHandler
level=ERROR
formatter=detailFomatter
args=('mail.google.cocom','from at miraelabs.com',['to at jarvisNim.google.com',],'ERROR!',('jarvisNim','alfoek224!'))
# args=('localhost', 'from@abc', ['user1@abc', 'user2@xyz'], 'Logger Subject')
kwargs={'timeout': 10.0}

[handler_reports]
class=FileHandler
level=DEBUG
formatter=detailFomatter
args=('./batch/reports/reports.log', 'a')  # mode = 'w' 는 업데이트 모드


[formatter_simpleFormatter]
format=%(asctime)s - %(name)s - %(levelname)s - %(message)s

[formatter_detailFomatter]
format=%(asctime)s - %(name)s - %(levelname)s - %(message)s
datefmt=%m-%d %H:%M